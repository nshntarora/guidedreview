import { CHROME_WEB_STORE_URL } from "@web/lib/links";

export const toc = [
  { id: "pick-a-host", label: "Pick a host", level: 2 },
  { id: "detailed-docs", label: "Detailed docs", level: 2 },
  { id: "after-you-install", label: "After you install", level: 2 },
];

# Install

Guided Review is one review engine with two hosts. Install the surface that matches the change you’re reviewing — or both; they share providers, units, and keyboard habits.

| Host                 | Use it when…                                      |
| -------------------- | ------------------------------------------------- |
| **CLI**              | You’re on a local branch, commit, or working tree |
| **Chrome extension** | The change is already a PR on `github.com`        |

Same clustering pipeline either way. The extension can [submit to GitHub](/docs/submit-review); the CLI keeps notes local and offers [Generate Prompt](/docs/generate-prompt). Motivation: [Why](/docs/why). Product map: [Introduction](/docs).

<TocCard toc={toc} />

## Pick a host

**CLI** — Node.js 22+, then:

```bash
npx @guided-review/cli
```

The published package is `@guided-review/cli`. The binary is `guidedreview`. `npx guided-review` (unscoped, hyphen) is a different tool. The CLI binds localhost, opens the browser UI, and walks file-by-file until you hit **Structure with AI**.

**Chrome extension** — Install from the <a href={CHROME_WEB_STORE_URL}>Chrome Web Store</a>. On a PR, click **Start Guided Review** (or the toolbar icon). Pin the extension so the icon stays visible.

A provider key is optional for a [file-by-file walk](/docs/how-it-works#without-a-provider); required for AI clustering and summaries — [Configure AI provider](/docs/configure-provider).

## Detailed docs

Full install steps, Options, flags, and day-two workflows live on the app pages:

<InstallAppCards />

## After you install

1. [Configure an AI provider](/docs/configure-provider) (extension Options or CLI Settings / env).
2. Extension path: [Your first review](/docs/first-review). CLI path: [CLI → Walk the plan](/docs/cli#walk-the-plan).
3. Optionally [connect GitHub](/docs/connect-github) if you’ll [submit from the extension](/docs/submit-review).

Stuck on a missing button, provider error, or port conflict: [Troubleshooting](/docs/troubleshooting).

**Next.** [Chrome Extension](/docs/chrome-extension) · [CLI](/docs/cli) · [Configure AI provider](/docs/configure-provider) · [Your first review](/docs/first-review)
